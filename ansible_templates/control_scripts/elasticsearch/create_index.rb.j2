require 'dataspects'

esc = Dataspects::ElasticsearchCluster.new(
  url: "http://localhost:9200"
)

esc.initialize_index(
  index: ENV['ES_INDEX_NAME'],
  settings: 'standard_index_settings.json',
  mapping: 'standard_index_mapping.json'
)
