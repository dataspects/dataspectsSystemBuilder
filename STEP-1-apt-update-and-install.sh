#!/bin/bash

ssh -p 2222 ds@localhost

# ds@dataspects:~$ sudo apt update && sudo apt upgrade && sudo apt install git docker-compose curl ruby lynx restic net-tools sshfs wget

# ds@dataspects:~$ sudo apt install virtualbox-guest-utils virtualbox-guest-additions-iso
# ds@dataspects:~$ sudo mount -o loop /usr/share/virtualbox/VBoxGuestAdditions.iso /media/ && sudo /media/VBoxLinuxAdditions.run